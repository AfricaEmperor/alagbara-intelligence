import './styles.css';
export const metadata={title:'ALAGBARA — Salt Intelligence — Reconstruction Kernel v0.1',description:'Epistemic control surface for salt trade reconstruction.'};
export const viewport={width:'device-width',initialScale:1};
export default function RootLayout({children}){return <html lang="en"><body>{children}</body></html>}